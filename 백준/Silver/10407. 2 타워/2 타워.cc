#include <bits/stdc++.h>

using namespace std;

long long H;

int main() {
    cin >> H;

    if(H == 1) {
        cout << "2" << "\n";
    }
    else {
        cout << "1" << "\n";
    }

    return 0;
}